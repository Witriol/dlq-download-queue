# SSH target for Unraid deployment
REMOTE_HOST=my-server

# Data volumes (DATA_* variables, each as host:container)
# Container paths become browsable in UI
# Add more DATA_xxx vars as needed, then add to your compose volumes
DATA_TVSHOWS=/mnt/user/tvshows:/data/tvshows
DATA_MOVIES=/mnt/user/movies:/data/movies

# State volume
STATE_MOUNT=/mnt/user/appdata/dlq:/state

# HTTP
DLQ_HTTP_PORT=8099
DLQ_WEBUI_PORT=8098
# Optional: bind host (default 0.0.0.0)
# DLQ_HTTP_HOST=0.0.0.0
# Optional: explicit host:port override (takes precedence if set)
# DLQ_HTTP_ADDR=0.0.0.0:8099

# Runtime
PUID=99
PGID=100
TZ=UTC

# Aria2 optional overrides
# ARIA2_RPC=http://127.0.0.1:6800/jsonrpc
# ARIA2_RPC_LISTEN_PORT=6800
# ARIA2_DIR=/data
# ARIA2_MAX_CONNECTION_PER_SERVER=4
# ARIA2_SHOW_CONSOLE_READOUT=false
