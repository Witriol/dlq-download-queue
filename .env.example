# SSH target for Unraid deployment
REMOTE_HOST=my-server

# Data volumes (DATA_* variables, each as host:container)
# Container paths become browsable in UI
# Add more DATA_xxx vars as needed, then add to docker-compose.yml volumes
DATA_TVSHOWS=/mnt/user/tvshows:/data/tvshows
DATA_MOVIES=/mnt/user/movies:/data/movies

# State volume
STATE_MOUNT=/mnt/user/appdata/dlq:/state

# Runtime
PUID=99
PGID=100
TZ=UTC
